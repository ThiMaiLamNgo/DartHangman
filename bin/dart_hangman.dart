
/*
import 'dart:io';
import 'dart:math';
void mai(){
  List <String> words =['orange', 'apple', 'pera', 'banana'];

  Random random = new Random();
  int i = random.nextInt(words.length);
  String chosenWord = words[i];
  print('The word has ${chosenWord.length} characters');
  String underscore = '_';
  print(underscore*chosenWord.length)

  print('Enter a character');
  String input = stdin.readLineSync().toLowerCase();

  for(input in chosenWord){
    if(input in chosenWord){

    }
  }


}

 */

